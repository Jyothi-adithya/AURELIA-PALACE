DATABASE_URL="mysql://user:password@localhost:3306/aurelia_palace"

# Generate a strong secret with:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="replace-with-64-char-random-hex-string"

PORT=5000
CLIENT_URL="http://localhost:5173"
NODE_ENV="development"

# Required for production seeding (seed.js will refuse without this when NODE_ENV=production)
ADMIN_SEED_PASSWORD="replace-with-strong-admin-password"

# Email (Resend)
RESEND_API_KEY="re_xxxxxxxxxxxxx"
EMAIL_FROM="Aurelia Palace <onboarding@resend.dev>"
